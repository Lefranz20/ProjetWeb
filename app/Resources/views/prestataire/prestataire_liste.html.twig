{% extends 'layout.html.twig' %}
{% block prestataires %}
    <table class="table table-hover">
        <thead>
        <tr>
            <th>Nom de l'Entreprise</th>
            <th>Web</th>
            <th>N° TVA</th>

        </tr>
        </thead>
        <tbody>
        {% for p in prestataires %}
            <tr>
                <td>{{ p.nomEntreprise }}</td>
                <td>{{ p.web }}</td>
                <td>{{ p.numeroTva }}</td>
                <td class=""><a href="{{ url('show_prestaire',{'slug' : p.pretataireSlug}) }}">detail</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {# navigation de la pagination #}
    <div class="navigation">
        {{ knp_pagination_render(prestataires) }}
    </div>
{% endblock %}